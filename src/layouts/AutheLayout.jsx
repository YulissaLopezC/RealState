import React from 'react'

const AutheLayout = ({children}) => {
    return (
        <div>
            Layout autenticacion
            <main>{children}</main>
        </div>
    )
}

export default AutheLayout
